# Test-only dependencies. The service itself needs nothing here.
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
-r requirements.txt

pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0
