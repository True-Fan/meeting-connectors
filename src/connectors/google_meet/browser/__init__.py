"""Chromium: how it is launched, and where its Google session lives.

* ``profile.py``  — the persistent profile, and the per-session working copies that
  make more than one concurrent meeting possible.
* ``launcher.py`` — the launch flags, and why each one is there.
"""
